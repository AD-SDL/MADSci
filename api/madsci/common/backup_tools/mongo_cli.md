Module madsci.common.backup_tools.mongo_cli
===========================================
MongoDB backup management commands.

Functions
---------

`load_config_file(config_path: str) ‑> dict`
:   Load configuration from JSON file.

`main_mongodb_backup() ‑> None`
:   Entry point for MongoDB backup CLI.
