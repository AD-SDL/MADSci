"""Instrument drivers for the {{ module_name | pascal_case }} module.

Place hardware drivers, communication libraries, and vendor-specific code here.
Use the ``private/`` subdirectory for proprietary or non-public drivers
(it is excluded from version control via .gitignore).
"""
