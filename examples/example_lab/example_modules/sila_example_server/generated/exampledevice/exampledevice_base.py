# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from datetime import timedelta
from typing import TYPE_CHECKING, Optional

from sila2.server import (
    FeatureImplementationBase,
    MetadataDict,
    ObservableCommandInstanceWithIntermediateResponses,
)

from .exampledevice_types import (
    CountDown_IntermediateResponses,
    CountDown_Responses,
    GenerateData_Responses,
    Greet_Responses,
)

if TYPE_CHECKING:
    from ...server import Server


class ExampleDeviceBase(FeatureImplementationBase, ABC):
    parent_server: Server

    CountDown_default_lifetime_of_execution: Optional[timedelta]

    def __init__(self, parent_server: Server):
        """
        A minimal SiLA2 feature for demonstrating MADSci SilaNodeClient capabilities. Provides an unobservable command, an observable command, and a readable property.
        """
        super().__init__(parent_server=parent_server)

        self.CountDown_default_lifetime_of_execution = None

    @abstractmethod
    def get_ServerUptime(self, *, metadata: MetadataDict) -> float:
        """
        The number of seconds since the server started. Demonstrates property reading via get_state().

        :param metadata: The SiLA Client Metadata attached to the call
        :return: The number of seconds since the server started. Demonstrates property reading via get_state().
        """

    @abstractmethod
    def GenerateData(
        self, NumBytes: int, *, metadata: MetadataDict
    ) -> GenerateData_Responses:
        """
        Generates a deterministic byte sequence of the given length. Demonstrates binary data handling and ActionFiles in SilaNodeClient.


        :param NumBytes: The number of bytes to generate.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Data: The generated binary data.


        """

    @abstractmethod
    def Greet(self, Name: str, *, metadata: MetadataDict) -> Greet_Responses:
        """
        Returns a greeting for the given name. Demonstrates unobservable (synchronous) command execution.


        :param Name: The name to greet.

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - Greeting: The greeting message.


        """

    @abstractmethod
    def CountDown(
        self,
        Count: int,
        *,
        metadata: MetadataDict,
        instance: ObservableCommandInstanceWithIntermediateResponses[
            CountDown_IntermediateResponses
        ],
    ) -> CountDown_Responses:
        """
        Counts down from the given number, pausing 1 second between each count. Demonstrates observable (long-running) command execution with status polling.


        :param Count: The number to count down from.

        :param metadata: The SiLA Client Metadata attached to the call
        :param instance: The command instance, enabling sending status updates to subscribed clients

        :return:

            - Message: A completion message when the countdown finishes.


        """
